<?php

function adminer_object() {
  class AdminerSoftware extends Adminer {
    function credentials() {
      return array('<%= @mysqld_host %>:<%= @mysqld_port %>', '<%= @mysqld_username %>', '<%= @mysqld_password %>');
    }
  }

  return new AdminerSoftware;
}

$_GET['username'] = '';

include_once './adminer.php';
