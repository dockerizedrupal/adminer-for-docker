adminer:
  image: viljaste/adminer:dev
  hostname: adminer
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - adminerdata
  environment:
    - SERVER_NAME=localhost
adminerdata:
  image: viljaste/data:dev
  hostname: adminerdata
  volumes:
    - /adminer
